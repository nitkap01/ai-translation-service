# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env & secrets
.env

# Model weight caches (never commit multi-GB weights).
# Anchored to repo root so it does NOT match the app/models/ source package.
/models/
*.bin
*.safetensors

# Generated audio from the end-to-end check
samples/

# OS
.DS_Store
